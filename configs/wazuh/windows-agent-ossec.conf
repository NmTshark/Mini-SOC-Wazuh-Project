<!--
  Safe example only. Merge the required blocks into the existing
  <ossec_config> element on the Windows endpoint.
  Never replace a production configuration without reviewing it first.
-->
<ossec_config>
  <localfile>
    <location>Microsoft-Windows-Sysmon/Operational</location>
    <log_format>eventchannel</log_format>
  </localfile>
</ossec_config>

